    pipeline {
        agent any
        stages {
            stage('Compile stage') {
                 echo 'Compile'
            }
        }

             stage('Testing stage') {
                 echo 'Test'
            }
        }

              stage('Deployment stage') {
                echo 'Deploy'
        }

      }

    }
